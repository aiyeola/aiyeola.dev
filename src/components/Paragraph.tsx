import Typography from "@material-ui/core/Typography";

//@ts-ignore
export default function Paragraph(props) {
  return (
    <Typography
      style={{
        marginTop: "2rem",
      }}
      {...props}
    />
  );
}
